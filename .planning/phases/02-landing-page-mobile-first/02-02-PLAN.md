---
phase: 02-landing-page-mobile-first
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - index.html
  - style.css
autonomous: true
requirements:
  - LAND-07

must_haves:
  truths:
    - "Seção 'Como Funciona' aparece no scroll entre o hero e os serviços"
    - "Três passos são visíveis com ícone, número, label e descrição"
    - "Em mobile os passos empilham verticalmente com gap de 24px"
    - "Em desktop os passos ficam lado a lado com linhas conectoras entre eles"
    - "Seção tem animação de scroll reveal ao entrar na viewport"
  artifacts:
    - path: "index.html"
      provides: "Seção #como-funciona com 3 steps"
      contains: "como-funciona-section"
    - path: "style.css"
      provides: "Estilos completos da seção Como Funciona"
      contains: ".steps-grid"
  key_links:
    - from: "index.html section#como-funciona"
      to: "style.css .como-funciona-section"
      via: "class attribute"
      pattern: "como-funciona-section"
    - from: "index.html .step-indicator"
      to: "style.css .step-indicator"
      via: "48px circle com var(--color-primary)"
      pattern: "step-indicator"
    - from: "index.html .reveal"
      to: "JS IntersectionObserver"
      via: "document.querySelectorAll('.reveal')"
      pattern: "class.*reveal"
---

<objective>
Adicionar a seção "Como Funciona" ao one-page scroll da landing page.

Purpose: Criar contexto para o visitante entender o processo de contratação antes de ver os serviços — 3 passos simples que reduzem fricção e incentivam o contato via WhatsApp.

Output:
- `index.html` com `&lt;section id="como-funciona" class="como-funciona-section reveal"&gt;` inserido após `&lt;/header&gt;` e antes de `&lt;main class="content"&gt;`, contendo os 3 steps com ícones Material Symbols, números, labels e descrições
- `style.css` com todos os estilos das classes novas: `.como-funciona-section`, `.steps-grid`, `.step`, `.step-indicator`, `.step-number`, `.step-body`, `.step-icon`, `.step-label`, `.step-desc`, `.step-connector`
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-landing-page-mobile-first/02-CONTEXT.md
@.planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md
@.planning/phases/02-landing-page-mobile-first/02-01-SUMMARY.md

<interfaces>
<!-- Contratos CSS do plano 01 (já adicionados ao style.css) -->
:root {
  /* Já existentes: */
  --color-primary: #197e63;
  --color-primary-strong: #146c43;
  --color-surface: #ffffff;
  --color-text: #343a40;
  --color-text-subtle: #6c757d;
  --shadow-soft: 0 10px 40px rgba(0, 0, 0, 0.06);
  --radius-large: 16px;
  /* Adicionados no plano 01: */
  --color-hero-gradient-start: #e8f5f0;
  --color-primary-light: #4fb09b;
}

/* Sistema de reveal (style.css linhas 32-41): */
.reveal { opacity: 0; transform: translateY(30px); transition: all 0.8s cubic-bezier(0.5, 0, 0, 1); }
.reveal.active { opacity: 1; transform: translateY(0); }

/* JS IntersectionObserver (index.html linha 584-597):
   Observa: document.querySelectorAll('.service-card, .btn-primary, .about-content, .footer-premium')
   ATENÇÃO: O seletor atual NÃO inclui '.reveal' genérico — precisará ser atualizado
   no plano 03 para incluir as novas seções. Por ora, adicionar '.reveal' class na seção
   é suficiente pois o plano 03 atualizará o observer. */

<!-- Estrutura atual do index.html — ponto de inserção: -->
&lt;/header&gt;        ← INSERIR SEÇÃO AQUI
&lt;main class="content"&gt;
  &lt;section class="links"&gt;...&lt;/section&gt;
  ...
&lt;/main&gt;

<!-- Material Symbols já carregados no head (index.html linha 24-26):
     Google Fonts Material Symbols Rounded — pode usar ícones: forum, search, verified -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar HTML da seção "Como Funciona"</name>
  <files>index.html</files>
  <read_first>
    - index.html (linhas 255-265) — ver onde termina o &lt;/header&gt; e começa o &lt;main class="content"&gt;
    - .planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md — seção "'Como Funciona' Section (new)" e "Step component structure"
  </read_first>
  <action>
Inserir o bloco HTML abaixo imediatamente após `&lt;/header&gt;` (linha 257) e antes de `&lt;main class="content"&gt;` (linha 259).

HTML a inserir (copiar exatamente, incluindo indentação de 8 espaços):

```html
        &lt;!-- Seção Como Funciona --&gt;
        &lt;section id="como-funciona" class="como-funciona-section reveal"&gt;
            &lt;h2 class="section-heading"&gt;Como Funciona&lt;/h2&gt;
            &lt;div class="steps-grid"&gt;
                &lt;!-- Passo 1 --&gt;
                &lt;div class="step"&gt;
                    &lt;div class="step-indicator"&gt;
                        &lt;span class="step-number"&gt;1&lt;/span&gt;
                    &lt;/div&gt;
                    &lt;div class="step-body"&gt;
                        &lt;span class="material-symbols-rounded step-icon" aria-hidden="true"&gt;forum&lt;/span&gt;
                        &lt;p class="step-label"&gt;Me chama no WhatsApp&lt;/p&gt;
                        &lt;p class="step-desc"&gt;Me conta o que precisa — regularização, aprovação ou projeto. Sem compromisso.&lt;/p&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
                &lt;!-- Conector (desktop) --&gt;
                &lt;div class="step-connector" aria-hidden="true"&gt;&lt;/div&gt;
                &lt;!-- Passo 2 --&gt;
                &lt;div class="step"&gt;
                    &lt;div class="step-indicator"&gt;
                        &lt;span class="step-number"&gt;2&lt;/span&gt;
                    &lt;/div&gt;
                    &lt;div class="step-body"&gt;
                        &lt;span class="material-symbols-rounded step-icon" aria-hidden="true"&gt;search&lt;/span&gt;
                        &lt;p class="step-label"&gt;Analiso seu caso&lt;/p&gt;
                        &lt;p class="step-desc"&gt;Avalio a situação do imóvel e indico o caminho mais rápido e econômico.&lt;/p&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
                &lt;!-- Conector (desktop) --&gt;
                &lt;div class="step-connector" aria-hidden="true"&gt;&lt;/div&gt;
                &lt;!-- Passo 3 --&gt;
                &lt;div class="step"&gt;
                    &lt;div class="step-indicator"&gt;
                        &lt;span class="step-number"&gt;3&lt;/span&gt;
                    &lt;/div&gt;
                    &lt;div class="step-body"&gt;
                        &lt;span class="material-symbols-rounded step-icon" aria-hidden="true"&gt;verified&lt;/span&gt;
                        &lt;p class="step-label"&gt;Cuido de tudo&lt;/p&gt;
                        &lt;p class="step-desc"&gt;Executo cada etapa — da prefeitura ao cartório — até seu imóvel estar 100% regularizado.&lt;/p&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
            &lt;/div&gt;
        &lt;/section&gt;

```

Não modificar nada fora deste bloco inserido.
  </action>
  <verify>
    <automated>grep -c "como-funciona-section" index.html</automated>
    Deve retornar 1. Adicionalmente:
    - grep -c "step-indicator" index.html — deve retornar 3 (um por passo)
    - grep -c "step-connector" index.html — deve retornar 2 (entre os 3 passos)
    - grep -c "Me chama no WhatsApp" index.html — deve retornar 1
    - grep -c "Analiso seu caso" index.html — deve retornar 1
    - grep -c "Cuido de tudo" index.html — deve retornar 1
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"como-funciona\"" index.html` retorna 1
    - `grep -c "class=\"como-funciona-section reveal\"" index.html` retorna 1
    - `grep -c "step-indicator" index.html` retorna 3
    - `grep -c "step-connector" index.html` retorna 2
    - `grep -c "forum" index.html` retorna pelo menos 1 (ícone do passo 1)
    - `grep -c "search" index.html` retorna pelo menos 1 (ícone do passo 2)
    - `grep -c "verified" index.html` retorna pelo menos 1 (ícone do passo 3)
    - Os textos exatos estão presentes: "Me conta o que precisa — regularização, aprovação ou projeto. Sem compromisso."
    - Os textos exatos estão presentes: "Avalio a situação do imóvel e indico o caminho mais rápido e econômico."
    - Os textos exatos estão presentes: "Executo cada etapa — da prefeitura ao cartório — até seu imóvel estar 100% regularizado."
    - A seção está posicionada ANTES de `&lt;main class="content"&gt;` (verificar ordem no arquivo)
  </acceptance_criteria>
  <done>Seção "Como Funciona" com 3 passos inserida no HTML entre o header e o main.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar CSS da seção "Como Funciona" ao style.css</name>
  <files>style.css</files>
  <read_first>
    - style.css (linhas 1-80) — ver :root vars, padrão de media queries existente, fim do arquivo
    - .planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md — seções "Component Contracts" para .como-funciona-section, .steps-grid, .step, .step-indicator, .step-connector, .step-icon, .step-label, .step-desc
  </read_first>
  <action>
Adicionar ao final de style.css (após todos os seletores existentes) o seguinte bloco CSS. Usar as CSS vars do :root — NUNCA valores hex hardcoded exceto onde especificado na UI-SPEC:

```css
/* ============================================
   FASE 2 — Seção Como Funciona
   ============================================ */

.como-funciona-section {
    background: var(--color-surface);
    border-radius: var(--radius-large);
    box-shadow: var(--shadow-soft);
    padding: 24px;
    margin-top: 48px;
    overflow: hidden;
}

@media (min-width: 640px) {
    .como-funciona-section {
        padding: 32px 40px;
    }
}

.como-funciona-section .section-heading {
    margin-top: 0;
    margin-bottom: 24px;
}

.steps-grid {
    display: flex;
    flex-direction: column;
    gap: 24px;
}

@media (min-width: 640px) {
    .steps-grid {
        flex-direction: row;
        align-items: flex-start;
        gap: 0;
    }
}

.step {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    flex: 1;
    gap: 12px;
}

.step-indicator {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--color-primary);
    color: #ffffff;
    display: grid;
    place-items: center;
    flex-shrink: 0;
    box-shadow: 0 4px 12px rgba(25, 126, 99, 0.25);
}

.step-number {
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1;
}

.step-body {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
}

.step-icon {
    font-size: 28px;
    color: var(--color-primary);
}

.step-label {
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
    line-height: 1.3;
}

.step-desc {
    font-size: 1rem;
    font-weight: 400;
    color: var(--color-text-subtle);
    margin: 4px 0 0;
    line-height: 1.5;
}

.step-connector {
    display: none;
}

@media (min-width: 640px) {
    .step-connector {
        display: block;
        height: 2px;
        background: rgba(19, 111, 92, 0.2);
        flex: 1;
        margin-top: 24px;
        align-self: flex-start;
    }
}
```
  </action>
  <verify>
    <automated>grep -c "como-funciona-section" style.css</automated>
    Deve retornar pelo menos 1. Adicionalmente:
    - grep -c "steps-grid" style.css — deve retornar pelo menos 1
    - grep -c "step-indicator" style.css — deve retornar pelo menos 1
    - grep -c "step-connector" style.css — deve retornar pelo menos 1
    - grep -c "#197e63\|#146c43\|#e8f5f0\|#f8f9fa\|#ffffff\|#343a40\|#6c757d" style.css — deve retornar 0 nas novas classes adicionadas (verificar visualmente que novas regras usam var())
  </verify>
  <acceptance_criteria>
    - `grep -c "como-funciona-section" style.css` retorna pelo menos 2 (mobile + desktop rule)
    - `grep -c "steps-grid" style.css` retorna pelo menos 2 (mobile + desktop rule)
    - `grep -c "step-indicator" style.css` retorna pelo menos 1
    - `grep -c "step-connector" style.css` retorna pelo menos 2 (display: none + media query)
    - `grep -c "margin-top: 48px" style.css` retorna pelo menos 1 (no .como-funciona-section)
    - `grep -c "var(--color-primary)" style.css` retorna pelo menos o número existente + 2 novos (step-indicator background e step-icon color)
    - `grep -c "var(--color-surface)" style.css` retorna pelo menos o número existente + 1 novo
    - `grep -c "var(--shadow-soft)" style.css` retorna pelo menos o número existente + 1 novo
    - `grep -c "var(--radius-large)" style.css` retorna pelo menos o número existente + 1 novo
    - O bloco CSS tem as regras `@media (min-width: 640px)` para `.como-funciona-section`, `.steps-grid` e `.step-connector`
  </acceptance_criteria>
  <done>style.css com todos os estilos da seção Como Funciona, responsivos (mobile-first com breakpoint 640px), usando apenas CSS vars do design system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor→page | Usuário final acessa página estática — nenhuma entrada de dados neste plano |
| HTML→CSS | Seletores CSS mapeiam para classes HTML — sem superfície de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Material Symbols CDN | accept | CDN do Google carregado via HTTPS; conteúdo são nomes de ícones (strings), sem execução |
| T-02-05 | Denial of Service | Scroll reveal IntersectionObserver | accept | Observer existente já estabelecido; adição de uma seção com .reveal não aumenta superfície de ataque |
</threat_model>

<verification>
Após execução do plano:
1. Abrir index.html no browser — scroll down deve revelar seção "Como Funciona" entre o hero e os serviços
2. Mobile (viewport 390px): 3 passos empilhados verticalmente, cada um com círculo verde, ícone, label em negrito, descrição em cinza
3. Desktop (viewport 768px+): 3 passos lado a lado com linhas conectoras horizontais entre eles
4. Cada passo deve ter animação suave de entrada (scroll reveal)
5. `grep -c "como-funciona-section" index.html` retorna 1
6. `grep -c "steps-grid" style.css` retorna pelo menos 2
</verification>

<success_criteria>
- Seção "Como Funciona" visível no scroll, posicionada após o hero e antes dos serviços
- 3 passos com textos exatos: "Me chama no WhatsApp", "Analiso seu caso", "Cuido de tudo"
- Layout mobile-first: coluna em telas pequenas, linha em telas ≥640px
- Ícones Material Symbols: forum, search, verified
- Círculos numerados 48px com background var(--color-primary)
- Linhas conectoras visíveis apenas em desktop
- Scroll reveal funcional via classe .reveal existente
- Nenhum valor hex hardcoded nos novos seletores CSS (exceto rgba() de sombras especificadas na UI-SPEC)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-landing-page-mobile-first/02-02-SUMMARY.md`
</output>
