---
phase: 02-landing-page-mobile-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - style.css
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-06
  - LAND-07

must_haves:
  truths:
    - "Página é indexável pelo Google (robots meta removida)"
    - "Title e meta description corretos aparecem em resultados de busca"
    - "Hero exibe fundo em degradê verde suave ao invés de branco sólido"
    - "Tagline estática exibe 'Regularização e aprovação de obras, do projeto à averbação' (sem marquee animado)"
    - "Botões WhatsApp e Área do Cliente permanecem funcionais no hero"
  artifacts:
    - path: "index.html"
      provides: "Hero renovado e SEO corrigido"
      contains: "hero-tagline-static"
    - path: "style.css"
      provides: "Novas CSS vars e estilo da tagline estática"
      contains: "--color-hero-gradient-start"
  key_links:
    - from: "style.css :root"
      to: ".hero background"
      via: "var(--color-hero-gradient-start)"
      pattern: "color-hero-gradient-start"
    - from: "index.html <head>"
      to: "Google Search"
      via: "title e meta description"
      pattern: "Vilela Engenharia.*Regularização"
---

<objective>
Renovar o hero da landing page e corrigir o SEO.

Purpose: Substituir a tagline marquee animada por tagline estática proeminente, adicionar degradê verde no hero, corrigir metatags para indexação, atualizar `<title>` e `<meta description>`, e adicionar novas CSS vars ao design system.

Output:
- `index.html` com metatags no-cache e noindex removidas, `<title>` e `<meta description>` atualizados, schema.org com campo description, tagline marquee removida e substituída por tagline estática, hero com classe de gradiente
- `style.css` com `--color-hero-gradient-start` e `--color-primary-light` em `:root`, estilo `.hero-tagline-static`, regra de gradiente no `.hero`, e `scroll-behavior: smooth` em `html`
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-landing-page-mobile-first/02-CONTEXT.md
@.planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md

<interfaces>
<!-- Contratos CSS existentes — ler antes de editar -->
<!-- De style.css :root (linhas 1-29): -->
:root {
    --color-bg: #f8f9fa;
    --color-surface: #ffffff;
    --color-surface-soft: #e9ecef;
    --color-primary: #197e63;
    --color-primary-strong: #146c43;
    --color-accent: #e8960f;
    --color-text: #343a40;
    --color-text-subtle: #6c757d;
    --shadow-soft: 0 10px 40px rgba(0, 0, 0, 0.06);
    --radius-large: 16px;
    --radius-small: 12px;
    --font-base: "Outfit", "Segoe UI", "Helvetica Neue", Arial, sans-serif;
}

<!-- De index.html, estrutura do hero (linhas 207-256): -->
<header class="hero" role="banner">
  <div class="header-profile-row">
    <picture class="logo">...</picture>
    <div class="engineer-info">
      <span class="label-responsibility">Engenheiro Responsável</span>
      <a href="#sobre" class="name-link"><h2>Diego T. N. Vilela</h2></a>
      <span class="crea-badge">Engenheiro Civil · CREA 235.474/D</span>
    </div>
  </div>
  <div class="engineer-card">
    <!-- REMOVER: .tagline-container + .tagline marquee -->
    <div class="tagline-container">...</div>
    <!-- MANTER: -->
    <div class="v-actions">
      <a class="v-btn-base v-btn-phone" href="https://wa.me/5535984529577?text=Ola%20Diego%20Vilela"...>
      <a class="v-btn-base v-btn-client" href="https://vilela.eng.br/contato/area-cliente/"...>
    </div>
  </div>
</header>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir SEO no &lt;head&gt; de index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (linhas 1-201) — ver metatags atuais, title, schema.org
    - .planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md — seção "SEO Contract"
  </read_first>
  <action>
Editar `index.html` realizando as seguintes mudanças no `&lt;head&gt;` (per D-03):

1. **Remover** as 4 metatags de no-cache e noindex (linhas 10-13):
   ```html
   &lt;meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate"&gt;
   &lt;meta http-equiv="Pragma" content="no-cache"&gt;
   &lt;meta http-equiv="Expires" content="0"&gt;
   &lt;meta name="robots" content="noindex, nofollow"&gt;
   ```

2. **Substituir** o `&lt;title&gt;` existente:
   - DE: `&lt;title&gt;Diego Vilela | Links oficiais&lt;/title&gt;`
   - PARA: `&lt;title&gt;Vilela Engenharia | Regularização e Aprovação de Imóveis em Minas Gerais&lt;/title&gt;`

3. **Adicionar** `&lt;meta name="description"&gt;` imediatamente após o `&lt;title&gt;`:
   ```html
   &lt;meta name="description" content="Regularização e aprovação de imóveis com segurança e agilidade. Projetos, alvará, averbação e CND — do projeto à averbação. Atendimento em Minas Gerais. CREA 235.474/D."&gt;
   ```

4. **Atualizar** o JSON-LD schema.org (dentro do `&lt;script type="application/ld+json"&gt;`) — adicionar campo `"description"` após a linha `"sameAs"`:
   ```json
   "description": "Regularização e aprovação de imóveis em Minas Gerais. Serviços de alvará, habite-se, averbação, CND, projetos de engenharia e consultoria técnica."
   ```
   O campo deve ser inserido antes do fechamento `}` do objeto raiz.
  </action>
  <verify>
    <automated>grep -c "noindex" index.html</automated>
    Deve retornar 0. Adicionalmente verificar:
    - grep "Vilela Engenharia | Regularização" index.html — deve encontrar no title
    - grep "meta name=\"description\"" index.html — deve retornar 1 resultado
    - grep "description.*Regularização e aprovação de imóveis em Minas Gerais" index.html — deve retornar 1 resultado no schema.org
  </verify>
  <acceptance_criteria>
    - `grep -c "noindex" index.html` retorna 0
    - `grep -c "no-cache" index.html` retorna 0
    - `grep -c "Vilela Engenharia | Regularização e Aprovação de Imóveis em Minas Gerais" index.html` retorna 1
    - `grep -c "name=\"description\"" index.html` retorna 1
    - `grep -c "CREA 235.474/D" index.html` retorna pelo menos 2 (meta description + crea-badge existente)
    - O JSON-LD contém `"description": "Regularização e aprovação de imóveis em Minas Gerais`
  </acceptance_criteria>
  <done>Head do index.html sem metatags de bloqueio de SEO, com title e meta description corretos, e schema.org com campo description.</done>
</task>

<task type="auto">
  <name>Task 2: Renovar hero — degradê, tagline estática e CSS vars</name>
  <files>index.html, style.css</files>
  <read_first>
    - index.html (linhas 118-256) — ver .tagline-container, .tagline, .engineer-card, .v-actions
    - style.css (linhas 1-80) — ver :root vars atuais, .hero, padrões existentes
    - .planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md — seções "Hero Section", "Component Contracts (.hero-tagline-static)", "New CSS Variables"
  </read_first>
  <action>
**Em style.css:**

1. Adicionar 2 novas vars ao final do bloco `:root` (antes do `}` de fechamento do :root):
   ```css
   /* New in Phase 2 */
   --color-hero-gradient-start: #e8f5f0;
   --color-primary-light: #4fb09b;
   ```

2. Adicionar `scroll-behavior: smooth` ao seletor `html` (criar seletor `html { scroll-behavior: smooth; }` após o bloco `:root`, antes ou após o bloco `.reveal`).

3. Localizar o seletor `.hero` em style.css (dentro do bloco `&lt;style&gt;` inline em index.html, linha ~163: `.hero { padding: 40px 28px 44px; }`). **NÃO** modificar esse seletor inline. Em vez disso, adicionar em style.css (o arquivo externo) uma regra que sobrescreve o background do hero:
   ```css
   .hero {
     background: linear-gradient(180deg, var(--color-hero-gradient-start) 0%, var(--color-surface) 100%);
   }
   ```
   Adicionar após a seção das variáveis :root e scroll-behavior.

4. Adicionar o estilo da tagline estática em style.css:
   ```css
   .hero-tagline-static {
     margin: 4px 0;
     font-size: 1rem;
     font-weight: 700;
     color: var(--color-primary);
     text-align: center;
     line-height: 1.4;
   }
   ```

**Em index.html:**

5. Remover o bloco completo `.tagline-container` e seu conteúdo (linhas ~230-236):
   ```html
   &lt;!-- Tagline Agora ACIMA dos botões com Marquee --&gt;
   &lt;div class="tagline-container"&gt;
       &lt;p class="tagline"&gt;Seu imóvel 100% regularizado...&lt;/p&gt;
   &lt;/div&gt;
   ```

6. Remover os estilos inline do `&lt;style&gt;` interno que controlam o marquee (linhas ~119-148):
   ```css
   /* Tagline Marquee */
   .tagline-container { ... }
   .tagline { ... }
   @keyframes scroll-left { ... }
   ```

7. No lugar do `.tagline-container` removido (dentro de `.engineer-card`, antes de `.v-actions`), inserir:
   ```html
   &lt;!-- Tagline estática (substituindo marquee) --&gt;
   &lt;p class="hero-tagline-static"&gt;Regularização e aprovação de obras, do projeto à averbação&lt;/p&gt;
   ```

8. Atualizar o `&lt;link rel="stylesheet" href="style.css?v=12"&gt;` para `style.css?v=13` (cache bust para a nova versão do arquivo).
  </action>
  <verify>
    <automated>grep -c "hero-tagline-static" index.html</automated>
    Deve retornar 1. Adicionalmente:
    - grep -c "tagline-container" index.html — deve retornar 0
    - grep -c "scroll-left" index.html — deve retornar 0
    - grep "color-hero-gradient-start" style.css — deve encontrar a var declarada em :root
    - grep "linear-gradient" style.css — deve encontrar a regra do hero gradient
    - grep "scroll-behavior" style.css — deve encontrar smooth
    - grep "hero-tagline-static" style.css — deve encontrar a classe
  </verify>
  <acceptance_criteria>
    - `grep -c "tagline-container" index.html` retorna 0
    - `grep -c "scroll-left" index.html` retorna 0
    - `grep -c "hero-tagline-static" index.html` retorna 1
    - O texto "Regularização e aprovação de obras, do projeto à averbação" está dentro do `&lt;p class="hero-tagline-static"&gt;`
    - `grep -c "color-hero-gradient-start: #e8f5f0" style.css` retorna 1
    - `grep -c "color-primary-light: #4fb09b" style.css` retorna 1
    - `grep -c "scroll-behavior: smooth" style.css` retorna 1
    - `grep -c "linear-gradient(180deg, var(--color-hero-gradient-start)" style.css` retorna 1
    - `grep -c "hero-tagline-static" style.css` retorna pelo menos 1 (a classe CSS declarada)
    - `grep -c "v=13" index.html` retorna 1 (cache bust atualizado)
    - Os botões `.v-btn-phone` e `.v-btn-client` permanecem intactos no hero (grep -c "v-btn-phone" index.html retorna 1)
  </acceptance_criteria>
  <done>Hero com degradê verde sutil via CSS var, tagline estática proeminente no lugar do marquee animado, novas CSS vars adicionadas ao design system, scroll suave habilitado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor→page | Usuário final acessa a landing page via HTTPS — conteúdo é estático, sem entrada de dados do usuário neste plano |
| head→crawler | Metatags expostas a crawlers do Google e redes sociais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Meta description | accept | Conteúdo público por design — remoção do noindex é o objetivo do plano |
| T-02-02 | Tampering | style.css CDN (Google Fonts) | accept | Carregado via HTTPS; CDN do Google é confiável para este porte |
| T-02-03 | Information Disclosure | schema.org JSON-LD | accept | Expõe apenas dados públicos (nome, telefone, endereço MG) — sem PII sensível |
</threat_model>

<verification>
Após execução do plano:
1. Abrir index.html no browser — hero deve exibir fundo com gradiente verde sutil (de #e8f5f0 a branco)
2. Tagline "Regularização e aprovação de obras, do projeto à averbação" deve aparecer em verde, estática, sem animação
3. Ver source da página — sem meta robots noindex, title correto, meta description presente
4. Clicar no botão WhatsApp — deve abrir wa.me/5535984529577
5. Clicar em "Cliente" — deve abrir area-cliente
6. `grep -c "noindex" index.html` retorna 0
</verification>

<success_criteria>
- Hero tem gradiente verde suave (não background branco sólido)
- Tagline estática visível no hero, sem animação de scroll
- Página remove todas as metatags de bloqueio de SEO (noindex, no-cache)
- `&lt;title&gt;` e `&lt;meta description&gt;` corretos para SEO de engenheiro civil MG
- Novas CSS vars `--color-hero-gradient-start` e `--color-primary-light` no :root de style.css
- `scroll-behavior: smooth` habilitado
- Botões WhatsApp e Área do Cliente intactos e funcionais
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-landing-page-mobile-first/02-01-SUMMARY.md`
</output>
