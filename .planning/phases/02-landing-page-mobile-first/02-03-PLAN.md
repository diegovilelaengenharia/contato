---
phase: 02-landing-page-mobile-first
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - index.html
  - style.css
autonomous: true
requirements:
  - LAND-04
  - LAND-05
  - LAND-06
  - LAND-07
  - LAND-08

must_haves:
  truths:
    - "Cada card de serviço exibe 2 linhas de descrição em cinza abaixo do subtitle"
    - "Seção 'Sobre Diego Vilela' aparece no scroll entre os serviços e o footer"
    - "Foto de Diego aparece circular na seção Sobre, ao lado do texto biográfico"
    - "Três badges de credencial (CREA, UFSJ, Pref. Oliveira) aparecem abaixo do texto"
    - "Footer 'Sobre Mim' é link âncora para #sobre, não botão que abre modal"
    - "bioModal e funções openBioModal/closeBioModal foram removidos"
    - "IntersectionObserver monitora todas as seções .reveal incluindo as novas"
    - "Modal de orçamento permanece intacto e funcional"
  artifacts:
    - path: "index.html"
      provides: "Card descriptions, seção Sobre, remoção de bioModal, footer corrigido"
      contains: "sobre-section"
    - path: "style.css"
      provides: "Estilos para .card-description, .sobre-section, .sobre-container, .sobre-img, .credential-badge"
      contains: ".credential-badge"
  key_links:
    - from: "index.html #sobre"
      to: "footer a[href='#sobre']"
      via: "âncora de navegação"
      pattern: "href=\"#sobre\""
    - from: "index.html .link-card"
      to: ".card-description"
      via: "span.text filho"
      pattern: "card-description"
    - from: "index.html assets/foto-diego-new.jpg"
      to: ".sobre-img"
      via: "src attribute"
      pattern: "foto-diego-new"
    - from: "JS IntersectionObserver"
      to: ".reveal elements"
      via: "querySelectorAll('.reveal')"
      pattern: "querySelectorAll.*reveal"
---

<objective>
Completar a landing page: adicionar descrições aos cards de serviços, criar seção "Sobre" inline, remover bioModal, corrigir footer e atualizar o IntersectionObserver para incluir todas as seções .reveal.

Purpose: Entregar o conteúdo real de apresentação de Diego (LAND-05) e as descrições dos serviços (LAND-04), removendo o bioModal obsoleto e garantindo que o scroll reveal funcione nas novas seções.

Output:
- `index.html` com `&lt;p class="card-description"&gt;` em cada um dos 5 cards de serviço; `&lt;section id="sobre"&gt;` completa com foto, 3 parágrafos, 3 badges; `#bioModal` removido; funções `openBioModal`/`closeBioModal` removidas; footer button substituído por âncora; IntersectionObserver atualizado
- `style.css` com classes: `.card-description`, `.sobre-section`, `.sobre-container`, `.sobre-photo`, `.sobre-img`, `.sobre-content`, `.sobre-text`, `.credential-badge`, `.credential-badges`
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-landing-page-mobile-first/02-CONTEXT.md
@.planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md
@.planning/phases/02-landing-page-mobile-first/02-02-SUMMARY.md

<interfaces>
<!-- Estrutura atual dos cards de serviço (index.html linhas 262-336): -->
<!-- Padrão de cada .link-card: -->
&lt;button class="link-card link-card--city" onclick="openModal('Prefeitura')"&gt;
    &lt;span class="icon" aria-hidden="true"&gt;
        &lt;span class="material-symbols-rounded"&gt;location_city&lt;/span&gt;
    &lt;/span&gt;
    &lt;span class="text"&gt;
        &lt;span class="title"&gt;Prefeitura&lt;/span&gt;
        &lt;span class="subtitle"&gt;Projetos de Regularização&lt;/span&gt;
        &lt;!-- INSERIR AQUI: &lt;p class="card-description"&gt;...&lt;/p&gt; --&gt;
    &lt;/span&gt;
&lt;/button&gt;

<!-- 5 cards que precisam de card-description: -->
<!-- 1. Prefeitura (onclick="openModal('Prefeitura')") -->
<!-- 2. Receita Federal (onclick="openModal('Receita Federal')") -->
<!-- 3. Cartório de Imóveis (onclick="openModal('Cartório de Imóveis')") -->
<!-- 4. Projetos de Engenharia (onclick="openModal('Projetos de Engenharia')") -->
<!-- 5. Consultoria e Laudos (onclick="openModal('Consultoria')") -->

<!-- Ponto de inserção da seção Sobre (index.html linha 337-338): -->
        &lt;/section&gt;  &lt;!-- fecha section.links de Consultoria --&gt;
        &lt;!-- INSERIR section#sobre AQUI --&gt;
    &lt;/main&gt;

<!-- Footer atual (index.html linhas 342-358): -->
&lt;footer class="footer-premium"&gt;
    &lt;div class="footer-premium__content"&gt;
        &lt;img src="assets/foto-diego-new.jpg" alt="Diego Vilela" class="footer-avatar"&gt;
        &lt;div class="footer-divider"&gt;&lt;/div&gt;
        &lt;div class="footer-info"&gt;
            &lt;span class="footer-label"&gt;ENGENHEIRO RESPONSÁVEL&lt;/span&gt;
            &lt;h3 class="footer-name"&gt;Diego T. N. Vilela&lt;/h3&gt;
            &lt;span class="footer-crea"&gt;CREA 235.474/D&lt;/span&gt;
            &lt;button class="footer-bio-link" onclick="openBioModal()"&gt;Sobre Mim&lt;/button&gt;  &lt;!-- MUDAR --&gt;
        &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class="footer-premium__bottom"&gt;
        &copy; &lt;span id="year"&gt;2026&lt;/span&gt; Vilela Engenharia
    &lt;/div&gt;
&lt;/footer&gt;

<!-- bioModal a remover (index.html linhas 411-440): -->
&lt;!-- Modal Biografia (NEW) --&gt;
&lt;div id="bioModal" class="modal" aria-hidden="true"&gt;...&lt;/div&gt;

<!-- Funções JS a remover (index.html linhas 524-535): -->
// Bio Modal Logic
function openBioModal() { ... }
function closeBioModal() { ... }

<!-- bioModal JS vars a remover (index.html linha 481): -->
const bioModal = document.getElementById('bioModal');
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar card-descriptions e seção Sobre ao index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (linhas 259-410) — ver todos os 5 cards de serviço e o ponto de inserção antes de &lt;/main&gt;
    - .planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md — seções "Serviços Section (modified)" e "'Sobre' Section (new)"
    - .planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md — seção "Copywriting Contract" para textos exatos
  </read_first>
  <action>
**Parte A — card-description em cada card de serviço:**

Para cada um dos 5 cards, adicionar `&lt;p class="card-description"&gt;` como último filho do `&lt;span class="text"&gt;`, APÓS o `&lt;span class="subtitle"&gt;`:

1. **Prefeitura** (após `&lt;span class="subtitle"&gt;Projetos de Regularização&lt;/span&gt;`):
   ```html
   &lt;p class="card-description"&gt;Alvará, habite-se, anistia e desmembramento — toda a burocracia municipal resolvida.&lt;/p&gt;
   ```

2. **Receita Federal** (após `&lt;span class="subtitle"&gt;CND e regularização de obras&lt;/span&gt;`):
   ```html
   &lt;p class="card-description"&gt;CNO, SERO e CND de obra para regularizar o INSS da construção.&lt;/p&gt;
   ```

3. **Cartório de Imóveis** (após `&lt;span class="subtitle"&gt;Averbação e escritura&lt;/span&gt;`):
   ```html
   &lt;p class="card-description"&gt;Averbação, retificação de área e registro — seu imóvel atualizado no cartório.&lt;/p&gt;
   ```

4. **Projetos de Engenharia** (após `&lt;span class="subtitle"&gt;Arquitetônico, Estrutural e mais&lt;/span&gt;`):
   ```html
   &lt;p class="card-description"&gt;Projetos arquitetônico, estrutural e complementares com ART registrada.&lt;/p&gt;
   ```

5. **Consultoria e Laudos** (após `&lt;span class="subtitle"&gt;Vistorias, Laudos e Perícia&lt;/span&gt;`):
   ```html
   &lt;p class="card-description"&gt;Laudos técnicos, vistorias e perícia para qualquer fase da obra.&lt;/p&gt;
   ```

Também adicionar a classe `.reveal` a cada `&lt;section class="links"&gt;` que ainda não a tenha:
- `&lt;section class="links"&gt;` → `&lt;section class="links reveal"&gt;`
(Aplicar nos 3 blocos de seção: Regularizações, Projetos de Engenharia e Consultoria Técnica)

**Parte B — Seção Sobre:**

Inserir o seguinte bloco HTML imediatamente antes de `&lt;/main&gt;` (após o fechamento da última `&lt;section class="links"&gt;`):

```html
            &lt;!-- Seção Sobre --&gt;
            &lt;section id="sobre" class="sobre-section reveal"&gt;
                &lt;h2 class="section-heading"&gt;Sobre Diego Vilela&lt;/h2&gt;
                &lt;div class="sobre-container"&gt;
                    &lt;div class="sobre-photo"&gt;
                        &lt;img src="assets/foto-diego-new.jpg" alt="Diego Vilela, Engenheiro Civil" class="sobre-img" loading="lazy"&gt;
                    &lt;/div&gt;
                    &lt;div class="sobre-content"&gt;
                        &lt;p class="sobre-text"&gt;Sou Diego Vilela, Engenheiro Civil formado pela Universidade Federal de São João Del Rei (UFSJ), especializado em regularização e aprovação de imóveis.&lt;/p&gt;
                        &lt;p class="sobre-text"&gt;Com experiência como Analista de Projetos na Prefeitura de Oliveira, conheço por dentro os trâmites da prefeitura, receita federal e cartório — o que agiliza cada etapa do processo para meus clientes.&lt;/p&gt;
                        &lt;p class="sobre-text"&gt;Meu compromisso é levar seu imóvel à regularização completa pelo caminho mais eficiente, evitando atrasos e custos desnecessários. Estou à disposição para cuidar do seu patrimônio.&lt;/p&gt;
                        &lt;div class="credential-badges"&gt;
                            &lt;span class="credential-badge"&gt;CREA 235.474/D&lt;/span&gt;
                            &lt;span class="credential-badge"&gt;Eng. Civil · UFSJ&lt;/span&gt;
                            &lt;span class="credential-badge"&gt;Ex-Analista de Projetos · Pref. Oliveira&lt;/span&gt;
                        &lt;/div&gt;
                    &lt;/div&gt;
                &lt;/div&gt;
            &lt;/section&gt;
```

**Parte C — Remover bioModal e corrigir footer:**

1. Remover o bloco completo do `#bioModal` (do comentário `&lt;!-- Modal Biografia (NEW) --&gt;` até o `&lt;/div&gt;` de fechamento inclusive).

2. Substituir no footer:
   - DE: `&lt;button class="footer-bio-link" onclick="openBioModal()"&gt;Sobre Mim&lt;/button&gt;`
   - PARA: `&lt;a href="#sobre" class="footer-bio-link"&gt;Sobre Mim&lt;/a&gt;`

3. Remover da seção `&lt;script&gt;` as seguintes partes:
   - Linha `const bioModal = document.getElementById('bioModal');`
   - O bloco de comentário `// Bio Modal Logic` e as duas funções completas `function openBioModal() {...}` e `function closeBioModal() {...}`

4. Atualizar o IntersectionObserver no `&lt;script&gt;` — localizar a linha:
   ```js
   const revealElements = document.querySelectorAll('.service-card, .btn-primary, .about-content, .footer-premium');
   ```
   Substituir por:
   ```js
   const revealElements = document.querySelectorAll('.reveal, .footer-premium');
   ```
   Isso garante que todas as seções com classe `.reveal` (como-funciona, links, sobre) sejam observadas.
  </action>
  <verify>
    <automated>grep -c "card-description" index.html</automated>
    Deve retornar 5. Adicionalmente:
    - grep -c "sobre-section" index.html — deve retornar 1
    - grep -c "bioModal" index.html — deve retornar 0
    - grep -c "openBioModal" index.html — deve retornar 0
    - grep -c "href=\"#sobre\"" index.html — deve retornar pelo menos 2 (footer + name-link existente no hero)
    - grep -c "querySelectorAll.*reveal" index.html — deve retornar 1
  </verify>
  <acceptance_criteria>
    - `grep -c "card-description" index.html` retorna 5
    - `grep -c "Alvará, habite-se, anistia" index.html` retorna 1
    - `grep -c "CNO, SERO e CND de obra" index.html` retorna 1
    - `grep -c "Averbação, retificação de área" index.html` retorna 1
    - `grep -c "Projetos arquitetônico, estrutural" index.html` retorna 1
    - `grep -c "Laudos técnicos, vistorias" index.html` retorna 1
    - `grep -c "id=\"sobre\"" index.html` retorna 1
    - `grep -c "sobre-section reveal" index.html` retorna 1
    - `grep -c "foto-diego-new.jpg" index.html` retorna 2 (sobre-img e footer-avatar)
    - `grep -c "credential-badge" index.html` retorna pelo menos 3 (um por badge) mais 1 para credential-badges container
    - `grep -c "CREA 235.474/D" index.html` retorna pelo menos 3 (hero crea-badge, sobre badge, footer)
    - `grep -c "Eng. Civil · UFSJ" index.html` retorna 1
    - `grep -c "Ex-Analista de Projetos · Pref. Oliveira" index.html` retorna 1
    - `grep -c "bioModal" index.html` retorna 0
    - `grep -c "openBioModal" index.html` retorna 0
    - `grep -c "closeBioModal" index.html` retorna 0
    - `grep -c "href=\"#sobre\".*footer-bio-link\|footer-bio-link.*href=\"#sobre\"" index.html` retorna 1
    - `grep -c "querySelectorAll.*\\.reveal" index.html` retorna 1
    - `grep -c "links reveal" index.html` retorna 3 (os 3 blocos de serviços)
    - O serviceModal e toda lógica openModal/closeModal/whatsappSubmit PERMANECE INTACTA (grep -c "openModal" index.html retorna pelo menos 5)
  </acceptance_criteria>
  <done>index.html com card-descriptions nos 5 serviços, seção Sobre completa com foto e badges, bioModal removido, footer com link âncora, IntersectionObserver observando todas as .reveal.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar CSS das seções Sobre e card-description ao style.css</name>
  <files>style.css</files>
  <read_first>
    - style.css — ler o final do arquivo (após os estilos de Como Funciona adicionados no plano 02) para inserir depois
    - .planning/phases/02-landing-page-mobile-first/02-UI-SPEC.md — seções "Component Contracts" para .card-description, .sobre-section, .sobre-container, .sobre-img, .sobre-text, .credential-badge, .credential-badges
  </read_first>
  <action>
Adicionar ao final de style.css (após o bloco "FASE 2 — Seção Como Funciona" adicionado no plano 02) o seguinte CSS:

```css
/* ============================================
   FASE 2 — Card Descriptions (Serviços)
   ============================================ */

.card-description {
    font-size: 1rem;
    color: var(--color-text-subtle);
    line-height: 1.5;
    margin: 4px 0 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

/* ============================================
   FASE 2 — Seção Sobre
   ============================================ */

.sobre-section {
    background: var(--color-surface);
    border-radius: var(--radius-large);
    box-shadow: var(--shadow-soft);
    padding: 24px;
}

@media (min-width: 640px) {
    .sobre-section {
        padding: 32px;
    }
}

.sobre-section .section-heading {
    margin-top: 0;
    margin-bottom: 20px;
}

.sobre-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    text-align: center;
}

@media (min-width: 640px) {
    .sobre-container {
        flex-direction: row;
        align-items: flex-start;
        gap: 32px;
        text-align: left;
    }
}

.sobre-photo {
    flex-shrink: 0;
}

.sobre-img {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    object-fit: cover;
    border: 4px solid var(--color-primary-light);
    box-shadow: 0 8px 24px -6px rgba(15, 111, 92, 0.25);
    display: block;
}

@media (min-width: 640px) {
    .sobre-img {
        width: 160px;
        height: 160px;
    }
}

.sobre-content {
    flex: 1;
}

.sobre-text {
    font-size: 1rem;
    color: var(--color-text);
    line-height: 1.6;
    margin: 0 0 12px;
}

.sobre-text:last-of-type {
    margin-bottom: 0;
}

.credential-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 16px;
    justify-content: center;
}

@media (min-width: 640px) {
    .credential-badges {
        justify-content: flex-start;
    }
}

.credential-badge {
    display: inline-block;
    padding: 4px 12px;
    border-radius: 999px;
    background: rgba(19, 111, 92, 0.10);
    color: var(--color-primary-strong);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.04em;
}
```
  </action>
  <verify>
    <automated>grep -c "card-description" style.css</automated>
    Deve retornar pelo menos 1. Adicionalmente:
    - grep -c "sobre-section" style.css — deve retornar pelo menos 1
    - grep -c "credential-badge" style.css — deve retornar pelo menos 2 (.credential-badge e .credential-badges)
    - grep -c "sobre-img" style.css — deve retornar pelo menos 2 (mobile + desktop)
    - grep -c "-webkit-line-clamp" style.css — deve retornar 1
  </verify>
  <acceptance_criteria>
    - `grep -c "card-description" style.css` retorna pelo menos 1
    - `grep -c "-webkit-line-clamp: 2" style.css` retorna 1
    - `grep -c "sobre-section" style.css` retorna pelo menos 2 (mobile + media query)
    - `grep -c "sobre-container" style.css` retorna pelo menos 2 (mobile + media query)
    - `grep -c "sobre-img" style.css` retorna pelo menos 2 (mobile + media query)
    - `grep -c "credential-badge" style.css` retorna pelo menos 2 (.credential-badge e .credential-badges)
    - `grep -c "999px" style.css` retorna pelo menos 1 (border-radius pill do badge)
    - `grep -c "var(--color-primary-light)" style.css` retorna pelo menos 1 (border da foto)
    - `grep -c "var(--color-primary-strong)" style.css` retorna pelo menos 1 (texto do badge)
    - `grep -c "justify-content: flex-start" style.css` retorna pelo menos 1 (badges desktop)
    - O arquivo tem `@media (min-width: 640px)` para `.sobre-section`, `.sobre-container`, `.sobre-img` e `.credential-badges`
  </acceptance_criteria>
  <done>style.css com todos os estilos de card-description e seção Sobre, mobile-first com breakpoint 640px, usando CSS vars do design system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor→page | Usuário final acessa página estática — nenhuma entrada de dados neste plano |
| visitor→WhatsApp | Clique em link-card abre modal → WhatsApp com mensagem pré-formatada (já existente, não modificado) |
| visitor→âncora | Footer "Sobre Mim" navega para #sobre via âncora — sem JavaScript, sem XSS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | serviceModal (orçamento) | accept | Modal e JS preservados exatamente como estão — nenhuma modificação neste plano |
| T-02-07 | Information Disclosure | foto-diego-new.jpg | accept | Imagem pública de apresentação profissional — intencional |
| T-02-08 | Spoofing | footer âncora #sobre | accept | Link local de mesma página — sem navegação externa, sem risco de open redirect |
| T-02-09 | Tampering | IntersectionObserver | accept | Observer atualizado usa seletor mais amplo (.reveal) — não aumenta superfície de ataque, apenas adiciona animação |
</threat_model>

<verification>
Após execução do plano:
1. Abrir index.html no browser — scroll reveal deve animar a seção "Sobre" e os cards de serviço
2. Cada card de serviço deve exibir 2 linhas de descrição em cinza abaixo do subtitle (Alvará... / CNO... / Averbação... / Projetos... / Laudos...)
3. Seção "Sobre Diego Vilela" visível com foto circular, 3 parágrafos, 3 badges verdes
4. Em mobile (390px): foto circular 120px centralizada, texto abaixo
5. Em desktop (768px+): foto 160px à esquerda, texto à direita
6. Footer "Sobre Mim" deve rolar suavemente até a seção Sobre (CSS scroll-behavior: smooth do plano 01)
7. Clicar em qualquer card de serviço deve abrir o modal de orçamento normalmente
8. `grep -c "bioModal" index.html` retorna 0
9. `grep -c "card-description" index.html` retorna 5
</verification>

<success_criteria>
- 5 cards de serviço com descrições de 2 linhas em var(--color-text-subtle)
- Seção "Sobre" com foto de Diego (120px/160px circular), 3 parágrafos biográficos e 3 badges de credencial
- bioModal completamente removido (HTML + JS)
- Footer "Sobre Mim" é link âncora `&lt;a href="#sobre"&gt;`
- IntersectionObserver observa `.reveal` genérico (inclui todas as novas seções)
- Modal de orçamento (serviceModal) preservado e funcionando
- Layout responsivo: coluna em mobile, linha em desktop (breakpoint 640px)
- Nenhuma função JS removida acidentalmente além de openBioModal/closeBioModal
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-landing-page-mobile-first/02-03-SUMMARY.md`
</output>
