---
phase: 01-seguranca-e-base
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-03
autonomous: true
files_modified:
  - area-cliente/admin_config.php
requirements:
  - SEC-02

must_haves:
  truths:
    - "admin_config.php não faz mais file_get_contents('db.php') nem preg_replace em db.php"
    - "Alteração de senha via admin_config.php grava o novo valor no arquivo area-cliente/.env"
    - "Se .env não existir, admin_config.php exibe mensagem de erro clara (não quebra silenciosamente)"
    - "Todo o resto de admin_config.php (configurações gerais, backup, limpeza) permanece intocado"
  artifacts:
    - path: "area-cliente/admin_config.php"
      provides: "Página de configuração do admin com lógica de alteração de senha corrigida"
      contains: "parse_ini_file"
  key_links:
    - from: "admin_config.php (POST update_password)"
      to: "area-cliente/.env"
      via: "parse_ini_file() + file_put_contents()"
      pattern: "file_put_contents.*\\.env"
---

<objective>
Corrigir o bloco de "Alterar Senha Admin" em admin_config.php para ler e gravar no arquivo area-cliente/.env em vez de fazer regex replace em db.php.

Purpose: Com o Plano 01, db.php deixou de conter a senha do admin. O mecanismo atual de admin_config.php (preg_replace no db.php) retornaria "Não foi possível localizar a definição de senha no arquivo db.php" para qualquer tentativa de alterar senha. O fix cirúrgico neste plano garante que a funcionalidade de troca de senha continue funcionando.

Output: admin_config.php com bloco update_password reescrito para ler/gravar no .env.
</objective>

<execution_context>
@C:/Users/diego/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-seguranca-e-base/01-CONTEXT.md
@.planning/phases/01-seguranca-e-base/01-RESEARCH.md
@.planning/phases/01-seguranca-e-base/01-01-SUMMARY.md

<interfaces>
<!-- Bloco a SUBSTITUIR em admin_config.php (linhas 19-45 do arquivo atual) -->

Código ATUAL (remover completamente):
```php
// 1. Alterar Senha Admin
if (isset($_POST['update_password'])) {
    $new_pass = trim($_POST['new_password']);
    if (strlen($new_pass) < 6) {
        $msg = "❌ A senha deve ter pelo menos 6 caracteres.";
    } else {
        // Read db.php
        $db_file = 'db.php';
        $content = file_get_contents($db_file);
        
        // Replace constant define('ADMIN_PASSWORD', '... Old ...');
        $pattern = "/define\('ADMIN_PASSWORD',\s*'([^']*)'\);/";
        $replacement = "define('ADMIN_PASSWORD', '$new_pass');";
        
        if (preg_match($pattern, $content)) {
            $new_content = preg_replace($pattern, $replacement, $content);
            if (file_put_contents($db_file, $new_content)) {
                $msg = "✅ Senha alterada com sucesso! A nova senha já está valendo.";
            } else {
                $msg = "❌ Erro ao escrever no arquivo db.php. Verifique as permissões.";
            }
        } else {
            $msg = "❌ Não foi possível localizar a definição de senha no arquivo db.php.";
        }
    }
}
```

Código NOVO (substituir o bloco acima):
```php
// 1. Alterar Senha Admin
if (isset($_POST['update_password'])) {
    $new_pass = trim($_POST['new_password']);
    if (strlen($new_pass) < 6) {
        $msg = "❌ A senha deve ter pelo menos 6 caracteres.";
    } else {
        $env_file = __DIR__ . '/.env';
        $env = parse_ini_file($env_file);
        if ($env === false) {
            $msg = "❌ Erro: Arquivo .env não encontrado. Contate o suporte.";
        } else {
            $env['ADMIN_PASSWORD'] = $new_pass;
            $lines = [];
            foreach ($env as $key => $value) {
                $lines[] = $key . '=' . $value;
            }
            if (file_put_contents($env_file, implode("\n", $lines) . "\n") !== false) {
                $msg = "✅ Senha alterada com sucesso! Faça logout e login para ativar a nova senha.";
            } else {
                $msg = "❌ Erro ao escrever no arquivo .env. Verifique as permissões (chmod 664).";
            }
        }
    }
}
```

Tudo que vem antes (linhas 1-18: ini_set, require, session check, $msg = '') e tudo que vem depois (linha 46 em diante: save_settings, backup, clean_logs, fetch settings, HTML) deve permanecer EXATAMENTE igual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Substituir bloco update_password em admin_config.php para usar .env</name>
  <files>area-cliente/admin_config.php</files>
  <read_first>
    - area-cliente/admin_config.php (ler arquivo completo — identificar linhas 19-45 do bloco update_password)
    - .planning/phases/01-seguranca-e-base/01-PATTERNS.md (seção "admin_config.php — FIX no bloco de senha")
    - .planning/phases/01-seguranca-e-base/01-RESEARCH.md (Pattern 4: Correção do admin_config.php)
  </read_first>
  <action>
Fazer uma substituição cirúrgica em admin_config.php: substituir apenas o bloco "// 1. Alterar Senha Admin" (if isset($_POST['update_password'])) pelo novo bloco que usa parse_ini_file() e file_put_contents() no .env.

**O que MUDA:**
- Remove: file_get_contents('db.php'), preg_match com padrão de define(), preg_replace, file_put_contents em db.php
- Adiciona: parse_ini_file(__DIR__ . '/.env'), loop para reescrever KEY=VALUE, file_put_contents no $env_file

**O que NÃO MUDA (não tocar):**
- Linhas 1-18: ini_set, require 'includes/init.php', require 'includes/schema.php', verificação de sessão, $msg = ''
- Linhas 47 em diante: todo o bloco save_settings, backup, clean_logs, fetch settings e o HTML completo
- Qualquer outra lógica do arquivo

**Conteúdo exato do novo bloco** (substitui completamente o bloco update_password atual):

```php
// 1. Alterar Senha Admin
if (isset($_POST['update_password'])) {
    $new_pass = trim($_POST['new_password']);
    if (strlen($new_pass) < 6) {
        $msg = "❌ A senha deve ter pelo menos 6 caracteres.";
    } else {
        $env_file = __DIR__ . '/.env';
        $env = parse_ini_file($env_file);
        if ($env === false) {
            $msg = "❌ Erro: Arquivo .env não encontrado. Contate o suporte.";
        } else {
            $env['ADMIN_PASSWORD'] = $new_pass;
            $lines = [];
            foreach ($env as $key => $value) {
                $lines[] = $key . '=' . $value;
            }
            if (file_put_contents($env_file, implode("\n", $lines) . "\n") !== false) {
                $msg = "✅ Senha alterada com sucesso! Faça logout e login para ativar a nova senha.";
            } else {
                $msg = "❌ Erro ao escrever no arquivo .env. Verifique as permissões (chmod 664).";
            }
        }
    }
}
```

**Aviso de comportamento em runtime:** A constante PHP `ADMIN_PASSWORD` é definida via `define()` em db.php e não pode ser redefinida na mesma requisição. Após gravar o novo .env, a constante na requisição atual ainda tem o valor antigo. Na próxima requisição (após logout/login), db.php relê o .env e a nova senha estará ativa. A mensagem de sucesso já orienta Diego a fazer logout/login.
  </action>
  <verify>
    <automated>grep -n "file_get_contents\|preg_match\|preg_replace\|db_file\|db\.php" area-cliente/admin_config.php</automated>
  </verify>
  <acceptance_criteria>
    - admin_config.php NÃO contém "file_get_contents($db_file)" ou "file_get_contents('db.php')"
    - admin_config.php NÃO contém "preg_match($pattern, $content)"
    - admin_config.php NÃO contém "preg_replace($pattern, $replacement, $content)"
    - admin_config.php NÃO contém "Não foi possível localizar a definição de senha no arquivo db.php"
    - admin_config.php contém "parse_ini_file($env_file)"
    - admin_config.php contém "__DIR__ . '/.env'"
    - admin_config.php contém "$env['ADMIN_PASSWORD'] = $new_pass"
    - admin_config.php contém "file_put_contents($env_file"
    - admin_config.php contém "chmod 664" (mensagem de erro de permissão)
    - admin_config.php ainda contém "require 'includes/init.php'" (início do arquivo preservado)
    - admin_config.php ainda contém "save_settings" (restante do arquivo preservado)
    - admin_config.php ainda contém "action=backup" (bloco de backup preservado)
  </acceptance_criteria>
  <done>admin_config.php corrigido: troca de senha agora lê/grava no .env em vez de fazer regex no db.php. Todo o restante do arquivo preservado intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → admin_config.php (POST) | Input do formulário de troca de senha — controlado pelo admin autenticado |
| admin_config.php → area-cliente/.env | Escrita de arquivo no servidor — requer permissão de escrita do processo PHP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | admin_config.php recebe nova senha sem validação de força | accept | Validação mínima de 6 caracteres mantida (strlen check); força de senha fica para fase futura (v2 req: 2FA admin) |
| T-04-02 | Information Disclosure | file_put_contents reescreve .env expondo outras credenciais (DB_PASS) se o arquivo for interceptado | accept | .env já está protegido por .htaccess (Plano 02) bloqueando acesso HTTP; arquivo em produção no servidor Hostinger |
| T-04-03 | Tampering | Loop de reescrita do .env sobrescreve chaves com valores incorretos se parse_ini_file() retornar array com estrutura inesperada | mitigate | Verificação $env === false antes de qualquer escrita; se parse falhar, mensagem de erro e nenhuma escrita ocorre |
| T-04-04 | Denial of Service | Permissões de arquivo (644 padrão via FTP) impedem file_put_contents de escrever no .env | accept | Mensagem de erro explícita instrui Diego a fazer chmod 664 via painel Hostinger; documentado na mensagem de erro do código |
</threat_model>

<verification>
Verificações locais (inspeção de código):

1. Bloco antigo removido:
   ```
   grep -c "preg_replace\|db_file\|file_get_contents" area-cliente/admin_config.php
   ```
   Deve retornar 0.

2. Novo bloco presente:
   ```
   grep -c "parse_ini_file\|env_file\|ADMIN_PASSWORD.*new_pass" area-cliente/admin_config.php
   ```
   Deve retornar pelo menos 2.

3. Arquivo preservado (bloco save_settings intacto):
   ```
   grep -c "save_settings\|action=backup\|clean_logs" area-cliente/admin_config.php
   ```
   Deve retornar 3.

Verificação funcional (pós-deploy, manual):
- Acessar https://vilela.eng.br/area-cliente/gestao_admin_99.php → admin_config.php
- Tentar alterar senha para "nova123" → deve ver mensagem de sucesso "✅ Senha alterada com sucesso! Faça logout e login..."
- Fazer logout e login com "nova123" → deve funcionar
- (Opcional: alterar senha de volta para a original)
</verification>

<success_criteria>
- admin_config.php não contém mais referências a db.php, preg_replace ou preg_match no bloco update_password
- admin_config.php contém parse_ini_file() e file_put_contents() apontando para __DIR__ . '/.env'
- Resto do arquivo (save_settings, backup, HTML) preservado exatamente
- Após deploy, troca de senha via interface admin funciona sem erro
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-seguranca-e-base/01-04-SUMMARY.md` com:
- Confirmação do bloco substituído (linhas antes e depois)
- Lista de acceptance_criteria verificados
- Nota sobre o comportamento esperado: "constante ADMIN_PASSWORD só reflete após logout/login"
</output>
