---
phase: 01-seguranca-e-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - area-cliente/db.php
  - area-cliente/.env.example
  - .gitignore
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "db.php não contém nenhuma credencial em plaintext (host, usuário, senha, senha admin)"
    - "db.php lê todas as credenciais de area-cliente/.env via parse_ini_file()"
    - "A interface pública de db.php é idêntica à anterior: variável $pdo e constante ADMIN_PASSWORD"
    - "area-cliente/.env.example existe no repositório com todas as chaves e valores placeholder"
    - ".gitignore não lista mais area-cliente/db.php (arquivo limpo pode ser commitado)"
    - ".gitignore lista explicitamente area-cliente/.env (arquivo real nunca commitado)"
  artifacts:
    - path: "area-cliente/db.php"
      provides: "Conexão PDO ao MySQL via variáveis de ambiente"
      contains: "parse_ini_file"
    - path: "area-cliente/.env.example"
      provides: "Template público com todas as chaves do .env"
      contains: "DB_HOST=srv1074.hstgr.io"
    - path: ".gitignore"
      provides: "Proteção contra commit de credenciais reais"
      contains: "area-cliente/.env"
  key_links:
    - from: "area-cliente/db.php"
      to: "area-cliente/.env"
      via: "parse_ini_file(__DIR__ . '/.env')"
      pattern: "parse_ini_file"
    - from: "includes/init.php, gestao_admin_99.php e outros"
      to: "area-cliente/db.php"
      via: "require_once 'db.php'"
      pattern: "ADMIN_PASSWORD"
---

<objective>
Reescrever db.php para ler credenciais de area-cliente/.env via parse_ini_file(), criar o template area-cliente/.env.example, e atualizar .gitignore para refletir a nova realidade (db.php limpo pode ser commitado; .env real nunca pode).

Purpose: Eliminar credenciais hardcoded de db.php — senha do banco e senha do admin estão atualmente em plaintext no arquivo que estava no .gitignore. Com esta mudança, db.php passa a ser seguro para commitar e as credenciais ficam apenas em .env (gerado em produção pelo deploy.yml no Plano 03).

Output: db.php reescrito (sem credenciais), area-cliente/.env.example (novo template), .gitignore atualizado.
</objective>

<execution_context>
@C:/Users/diego/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/diego/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seguranca-e-base/01-CONTEXT.md
@.planning/phases/01-seguranca-e-base/01-RESEARCH.md

<interfaces>
<!-- Interface pública que db.php DEVE preservar após a reescrita -->
<!-- Consumida por require_once 'db.php' em todos os arquivos PHP do projeto -->

Variáveis/constantes que os consumidores dependem:
  $pdo       — objeto PDO com conexão ativa ao MySQL
  ADMIN_PASSWORD — constante PHP definida via define()

Padrão PDO a PRESERVAR (linhas 12-24 do db.php atual):
```php
$dsn = "mysql:host=$host;dbname=$db;charset=$charset";
$options = [
    PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
    PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
    PDO::ATTR_EMULATE_PREPARES   => false,
];

try {
    $pdo = new PDO($dsn, $user, $pass, $options);
} catch (\PDOException $e) {
    throw new \PDOException($e->getMessage(), (int)$e->getCode());
}
```

Chaves que .env.example DEVE documentar (D-02, D-09):
  DB_HOST, DB_NAME, DB_USER, DB_PASS, ADMIN_PASSWORD
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Reescrever db.php para usar parse_ini_file()</name>
  <files>area-cliente/db.php</files>
  <read_first>
    - area-cliente/db.php (estado atual — ler para confirmar estrutura antes de reescrever)
    - area-cliente/db.example.php (padrão PDO de referência)
    - .planning/phases/01-seguranca-e-base/01-PATTERNS.md (seção "area-cliente/db.php — REESCRITA")
  </read_first>
  <action>
Reescrever area-cliente/db.php completamente. O novo arquivo DEVE:

1. Abrir com parse_ini_file() apontando para __DIR__ . '/.env'
2. Verificar se parse_ini_file() retornou false (falha crítica) — retornar HTTP 503 com mensagem genérica
3. Extrair as 4 variáveis de banco + charset fixo utf8mb4
4. Definir constante ADMIN_PASSWORD a partir de $env['ADMIN_PASSWORD']
5. Manter o bloco PDO exatamente igual ao atual (DSN, $options com ERRMODE_EXCEPTION + FETCH_ASSOC + EMULATE_PREPARES false, try/catch com rethrow)

Conteúdo exato do novo arquivo:

```php
<?php
// Configuração carregada de area-cliente/.env via parse_ini_file()
// Nunca edite credenciais diretamente aqui — edite o arquivo .env
$env = parse_ini_file(__DIR__ . '/.env');

if ($env === false) {
    http_response_code(503);
    die('Erro de configuração do servidor. Contate o suporte.');
}

$host    = $env['DB_HOST']    ?? '';
$db      = $env['DB_NAME']    ?? '';
$user    = $env['DB_USER']    ?? '';
$pass    = $env['DB_PASS']    ?? '';
$charset = 'utf8mb4';

// Configurações Gerais
define('ADMIN_PASSWORD', $env['ADMIN_PASSWORD'] ?? '');

$dsn = "mysql:host=$host;dbname=$db;charset=$charset";
$options = [
    PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
    PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
    PDO::ATTR_EMULATE_PREPARES   => false,
];

try {
    $pdo = new PDO($dsn, $user, $pass, $options);
} catch (\PDOException $e) {
    // Em produção, a mensagem será capturada pelo index.php
    throw new \PDOException($e->getMessage(), (int)$e->getCode());
}
```

NÃO incluir: host hardcoded, senha hardcoded, usuario hardcoded, define('ADMIN_PASSWORD', 'VilelaAdmin2025') ou qualquer valor real.
  </action>
  <verify>
    <automated>grep -n "parse_ini_file\|DB_HOST\|DB_PASS\|ADMIN_PASSWORD" "area-cliente/db.php"</automated>
  </verify>
  <acceptance_criteria>
    - area-cliente/db.php contém a string "parse_ini_file(__DIR__ . '/.env')"
    - area-cliente/db.php contém "$env['DB_HOST']"
    - area-cliente/db.php contém "$env['DB_PASS']"
    - area-cliente/db.php contém "$env['ADMIN_PASSWORD']"
    - area-cliente/db.php contém "PDO::ATTR_ERRMODE"
    - area-cliente/db.php NÃO contém "srv1074.hstgr.io" como literal
    - area-cliente/db.php NÃO contém "Diego@159753"
    - area-cliente/db.php NÃO contém "VilelaAdmin2025"
    - area-cliente/db.php NÃO contém "u884436813_vilela"
    - area-cliente/db.php contém "http_response_code(503)"
  </acceptance_criteria>
  <done>db.php reescrito sem nenhuma credencial em plaintext, lendo tudo de .env via parse_ini_file(), com interface $pdo e ADMIN_PASSWORD preservadas.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar area-cliente/.env.example e atualizar .gitignore</name>
  <files>area-cliente/.env.example, .gitignore</files>
  <read_first>
    - .gitignore (estado atual — identificar linha "area-cliente/db.php" a remover)
    - area-cliente/db.example.php (padrão de template público do projeto)
    - .planning/phases/01-seguranca-e-base/01-PATTERNS.md (seção ".gitignore — ATUALIZAÇÃO" e "area-cliente/.env.example — NOVO")
  </read_first>
  <action>
**Parte A — Criar area-cliente/.env.example:**

Criar o arquivo area-cliente/.env.example com o conteúdo abaixo (formato INI puro, sem PHP):

```ini
# area-cliente/.env.example
# Renomeie para .env e preencha com os valores reais
# NUNCA commite o arquivo .env com valores reais

DB_HOST=srv1074.hstgr.io
DB_NAME=u884436813_nome_do_banco
DB_USER=u884436813_usuario
DB_PASS=SUA_SENHA_DO_BANCO_AQUI
ADMIN_PASSWORD=SUA_SENHA_ADMIN_AQUI
```

**Parte B — Atualizar .gitignore:**

Aplicar três mudanças no .gitignore:

1. REMOVER a linha "area-cliente/db.php" e o comentário acima dela ("# Credenciais reais — use db.example.php como template"). O novo db.php é seguro para commitar.

2. ADICIONAR após a linha ".env" existente (linha 17) a entrada explícita:
```
# .env gerado pelo deploy — credenciais reais
area-cliente/.env
```

3. ADICIONAR no bloco de debug files (após "area-cliente/seed_test_user.php") as entradas faltantes:
```
area-cliente/session_test_*.php
area-cliente/probe.php
area-cliente/hello.php
```

Garantir que o .gitignore final:
- Não contenha mais "area-cliente/db.php"
- Contenha "area-cliente/.env"
- Contenha "area-cliente/session_test_*.php"
  </action>
  <verify>
    <automated>grep -n "area-cliente/db.php\|area-cliente/.env\|area-cliente/session_test" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo area-cliente/.env.example existe e contém "DB_HOST=srv1074.hstgr.io"
    - Arquivo area-cliente/.env.example contém "DB_PASS=SUA_SENHA_DO_BANCO_AQUI"
    - Arquivo area-cliente/.env.example contém "ADMIN_PASSWORD=SUA_SENHA_ADMIN_AQUI"
    - Arquivo area-cliente/.env.example NÃO contém "Diego@159753"
    - Arquivo area-cliente/.env.example NÃO contém "VilelaAdmin2025"
    - .gitignore NÃO contém a linha "area-cliente/db.php"
    - .gitignore contém "area-cliente/.env"
    - .gitignore contém "area-cliente/session_test_*.php"
    - .gitignore contém "area-cliente/probe.php"
  </acceptance_criteria>
  <done>Template .env.example commitado com todas as chaves e valores placeholder. .gitignore atualizado: db.php removido, area-cliente/.env adicionado, entradas de debug faltantes adicionadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo → código fonte | Qualquer pessoa com acesso ao repositório pode ler todos os arquivos commitados |
| servidor HTTP → arquivos PHP | Requests HTTP diretos a db.php expõem código-fonte se Apache não estiver configurado corretamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | area-cliente/db.php (credenciais hardcoded no git) | mitigate | db.php reescrito sem credenciais; parse_ini_file() lê .env que não é commitado (ASVS V2.10) |
| T-01-02 | Information Disclosure | ADMIN_PASSWORD hardcoded em db.php | mitigate | ADMIN_PASSWORD movido para .env; define() recebe $env['ADMIN_PASSWORD'] sem valor literal |
| T-01-03 | Information Disclosure | area-cliente/.env commitado por acidente | mitigate | area-cliente/.env adicionado explicitamente ao .gitignore neste plano |
| T-01-04 | Spoofing | Credenciais de banco em plaintext permitem acesso direto ao banco | mitigate | Após esta mudança, credenciais existem apenas em .env gerado em produção (não no código) |
</threat_model>

<verification>
Após execução deste plano:

1. Inspeção de código — db.php:
   ```
   grep -n "srv1074\|Diego@\|VilelaAdmin\|u884436813_vilela" area-cliente/db.php
   ```
   Deve retornar vazio (nenhuma credencial real no arquivo).

2. Inspeção de código — db.php tem parse_ini_file:
   ```
   grep -c "parse_ini_file" area-cliente/db.php
   ```
   Deve retornar 1.

3. Verificação do .gitignore:
   ```
   grep "area-cliente/db.php" .gitignore
   ```
   Deve retornar vazio.

4. Verificação do .gitignore:
   ```
   grep "area-cliente/.env" .gitignore
   ```
   Deve retornar a linha "area-cliente/.env".

5. Verificação do .env.example:
   ```
   grep "DB_HOST\|DB_PASS\|ADMIN_PASSWORD" area-cliente/.env.example
   ```
   Deve retornar as 5 chaves com valores placeholder.
</verification>

<success_criteria>
- db.php não contém nenhuma das strings: "srv1074.hstgr.io", "Diego@159753", "VilelaAdmin2025", "u884436813_vilela"
- db.php contém "parse_ini_file(__DIR__ . '/.env')"
- db.php contém "http_response_code(503)" (guarda contra .env ausente)
- area-cliente/.env.example existe no repositório com os 5 campos documentados
- .gitignore não lista "area-cliente/db.php"
- .gitignore lista "area-cliente/.env"
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-seguranca-e-base/01-01-SUMMARY.md` com:
- Arquivos modificados e o que mudou em cada um
- Confirmação de que interface $pdo e ADMIN_PASSWORD foram preservadas
- Status de cada acceptance_criteria
</output>
