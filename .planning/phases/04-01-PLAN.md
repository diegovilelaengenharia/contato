# Plano de Execução — Fase 4: Desmembramento do Backend Admin (Actions)

**Objetivo:** Extrair os handlers POST do monolito `processamento.php` para arquivos individuais em `actions/admin/`, utilizando a camada `core/` e validando CSRF.

## 1. Preparação
- [ ] Criar estrutura de pastas: `area-cliente/actions/admin/`
- [ ] Garantir que `.htaccess` protege a pasta `actions/` (já verificado)

## 2. Extração de Actions (Onda 1: Gestão de Processo)
- [ ] `actions/admin/processo_header_update.php` — Extraído de `update_processo_header`
- [ ] `actions/admin/etapa_update.php` — Extraído de `atualizar_etapa`
- [ ] `actions/admin/movimento_create.php` — Extraído de `adicionar_movimento` (ou similar)
- [ ] `actions/admin/movimento_delete.php`

## 3. Extração de Actions (Onda 2: Financeiro e Pendências)
- [ ] `actions/admin/financeiro_create.php`
- [ ] `actions/admin/financeiro_update_status.php`
- [ ] `actions/admin/financeiro_delete.php`
- [ ] `actions/admin/pendencia_create.php`
- [ ] `actions/admin/pendencia_resolve.php`
- [ ] `actions/admin/pendencia_delete.php`

## 4. Extração de Actions (Onda 3: Clientes e Documentos)
- [ ] `actions/admin/cliente_create.php`
- [ ] `actions/admin/cliente_update.php`
- [ ] `actions/admin/cliente_delete.php`
- [ ] `actions/admin/documento_upload.php`
- [ ] `actions/admin/documento_delete.php`
- [ ] `actions/admin/campos_extras_save.php`

## 5. Integração e Validação
- [ ] Atualizar formulários no Admin para apontar para as novas URLs
- [ ] Injetar campos CSRF nos formulários usando `Csrf::getHtmlField()`
- [ ] Testar cada fluxo end-to-end
- [ ] Limpar `processamento.php` após migração total

## Padrão de Implementação de uma Action:
```php
<?php
require_once __DIR__ . '/../../includes/init.php';
require_once __DIR__ . '/../../core/Auth.php';
require_once __DIR__ . '/../../core/Csrf.php';

// 1. Validar Auth Admin
if (!Auth::isAdmin()) {
    header('Location: ../../index.php');
    exit;
}

// 2. Validar CSRF
if (!Csrf::validateToken($_POST['csrf_token'] ?? '')) {
    die("Erro de validação CSRF.");
}

// 3. Processar Dados...
// 4. Redirecionar com mensagem
```
